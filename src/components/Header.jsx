import React from 'react';

const Header = () => (
  <header className="header">
    <div className="logo">ROSS NEWKIRK</div>
    <nav>
      <a className="learn-more" href="#description">Learn More</a>
      <a className="sign-up" href="#form-section">Sign-up</a>
    </nav>
  </header>
);

export default Header;
